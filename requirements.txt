# Audio processing and transcription dependencies
transformers==4.36.2
torch>=2.0.0
torchaudio>=2.0.0

# Faster Whisper using CTranslate2 (2-4x faster than transformers)
faster-whisper>=1.0.0

# MLX Whisper for Apple Silicon (Metal GPU acceleration)
mlx-whisper>=0.1.0

# OpenAI API for Whisper transcription
openai

# Audio processing and format conversion
librosa>=0.10.0
soundfile>=0.12.0

# Norwegian Whisper model dependencies
datasets>=2.14.0
accelerate>=0.24.0

# Optional: Jupyter notebook support (for misc/experimental.ipynb)
jupyter>=1.0.0
ipykernel>=6.25.0

# Video/audio download tool
yt-dlp>=2024.0.0

# Progress bar for downloads
tqdm>=4.65.0